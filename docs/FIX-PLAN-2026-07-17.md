# Fix Plan — 2026-07-17 (defect-driven, post bench-13)

State: bench-13 mean ssim 0.848, 10/16 QA pass, 5/16 contract pass. Suite: 1947 pass, 8 TDD
fails in tests/test_plate_integrity.py. HEAD 20d5373 carries unverified groundwork.

The user's bar, in order: (1) no visible defects — clipping, spurious rotation, strike
overreach, double text/ghosts, off-center button text, slice seams; (2) max editability —
text native, badges separate with real text, clean layer tree; (3) fonts consistent within a
hierarchy. Raw SSIM is a report, not the objective.

## Defect classes → root cause → fix → owner

| # | Defect | Root cause (file:line) | Fix | Wave |
|---|--------|------------------------|-----|------|
| P0-1 | 066 ✗ overlaps list text | icon_detect publishes meta.row.text_clip_x; no consumer | build_design_json: clip row text box after _generous_text_box (~L1992), guard RIGHT align, keep w ≥ prefit ink w | W1-A |
| P0-2 | Spurious rotations (025 2.35°, 088 4.42°) | merge_layers._text_sources re-averages line angles (L2057), discarding text_analysis consensus; single uncorroborated lines keep small angles | Use text_analysis rotation_deg consensus; snap 0<\|a\|<6° to 0 unless corroborated (block-mate or carrier median agrees). Keep genuine tilts (013 badge) | W1-B |
| P0-3 | 025 three emoji rows glued into one node | layout._semantic_text_stacks: shared block_id bypasses the gap test (L1575-1578) | Gap test applies even when block_id matches; split icon/emoji-led rows | W1-C |
| P0-4 | 025 "Blocks everything" ghost double | _smear_residue returns None on textured/gradient plates (reconstruct.py:1167-1168) — row never cleaned | Gradient-tolerant verdict via _gradient_plate_fill bg model (L1189) instead of None; then solid-fill clean | W1-E |
| P0-5 | 088 SALE broken E / slice seams | ownership hands each slice part of its glyph ink; full ink removed → plate shows through seams | Verify +81 groundwork (reconstruct.py:3705-3734): unit tests, config keys documented, role allowlist check; GPU 088 run | W1-F1 (tests) / W3 (GPU) |
| P0-6 | 002 €63 strike hits whole line | decorationSpan emitted but Figma plugin ignores it (code.js:1250 whole-node decoration) | Plugin: setRangeTextDecoration for decorationSpan; keep preview parity; clean baked strike from plate | W2-D |
| P0-7 | 067 baseline bias | landed in 20d5373 (leadingTrim=CAP_HEIGHT parity), unverified | Full verification: per-line bias table, 002/016/091/094 worst regions, GPU re-run | W3 |
| TDD | 8 plate-integrity fails | peel/inpaint routing: generative fill chosen for flat text holes + photographic product punches | Implement routing honesty in peel_scene/inpaint/reconstruct until tests/test_plate_integrity.py green | W2-G |
| Q-1 | Harness steers on SSIM; detected mistakes unfixable | reward = 0.55 contract + SSIM/LPIPS ladder; zero defect-class counters; repair vocabulary is stage-reruns only | New src/qa_defects.py: render-OCR per-line {ok,clipped,duplicated,garbled,missing} + rotation-anomaly, strike-overreach, double-text, centering, font-consistency counters → qa.json + acceptance gate + reward term | W2-H |
| Q-2 | Fixer has no element-level levers | harness.ACTIONABLE = stage reruns; HARNESS-PHASE2 operators unimplemented | Element repairs: snap_rotation, unclip_text, force_align_center, dedupe_text, strike_fix — wired critic→repair→verify | W2-I |
| F-1 | Same hierarchy, different fonts | per-line match variance; consensus passes post-hoc and family-only | Per-block majority vote on fontCandidates[0] in _unify_block_families; feed largest word box to matcher | W2-J1 |
| F-2 | 135 letterspacing; tracked caps render narrow | letterSpacing measured then forced to 0 (text_analysis.py:2940, build L1931) | Emit measured letterSpacing when tracking estimate confident; stop absorbing width error into font shrink | W2-J1 |
| F-3 | Button text not centered | alignment from canvas margins only; _is_centered reads loose paint box | button_shell host → force align CENTER, box centered on shell; ink-box centering in layout._is_centered | W2-J2 |
| P1 | 013 badge weight | corpus lacks the condensed face; wght axis only for name-resolved faces | Install condensed OFL faces into ~/.cache/google-fonts; drive wght for matcher-chosen variable faces | W3 |
| P1 | 016 seal promotion / 101 wordmark emission | seal has no carrier (needs segmentation); wordmark DIFF B ready needs landing with suppression | Assess on GPU pass; land if low-risk | W3 |
| P2 | Figma export never scored; VLM eviction; marquee strips | preview≠Figma divergence; model eviction root cause unknown | One manual plugin round-trip; VLM residency logging; defer marquee | backlog |

## Waves

- W1 (parallel, CPU): A clip consumer · B rotation · C row split · E ghost · F1 seam tests+config
- W2 (parallel, CPU): D strike spans · G plate-integrity TDD · H defect QA · I element repairs · J1 fonts · J2 centering
- W3 (serial, GPU): doctor → 067/088/025/002/066 targeted reruns → bench-14 full → compare defect counters vs bench-13
- Loop: bench-N → defect ledger → targeted fixes → rerun until defect counters 0 and no SSIM/contract regression vs bench-13 (ssim ≥ 0.848 mean, QA ≥ 10/16, contract ≥ 5/16, target contract ≥ 10/16)

## Invariants (do not regress)

- Text stays native; wordmarks stay artwork; photos stay alpha raster. No whole-image tracing.
- One union removal mask, one inpaint. Untouched source is never the background.
- preview == plugin == Figma: leadingTrim=CAP_HEIGHT parity (20d5373) must hold.
- Suite green: 1947+ pass; the 8 TDD fails flip to pass in W2-G, nothing else may break.
- GPU runs serialize (single RTX 5080). CPU agents never invoke the pipeline.
