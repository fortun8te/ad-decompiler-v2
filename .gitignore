# runs & caches
/runs/*
!/runs/.gitkeep
**/.cache/
figma-inbox/

# local benchmark images (real third-party ad creatives -- not for the repo)
/benchmark_set/

# models & binaries (download on the target box)
*.onnx
*.pth
*.safetensors
/.bin/
vtracer
vtracer.exe
potrace
potrace.exe

# python
__pycache__/
*.pyc
.venv/
venv/
.env
config.yaml

# os
.DS_Store
Thumbs.db

# scratch/working directory (GPU lock, local tool clones, dev artifacts)
/work/
